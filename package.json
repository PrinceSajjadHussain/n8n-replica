{
  "name": "flowforge",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev:api": "pnpm --filter @flowforge/api dev",
    "dev:worker": "pnpm --filter @flowforge/worker dev",
    "dev:web": "pnpm --filter @flowforge/web dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
