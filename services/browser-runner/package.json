{
  "name": "@flowforge/browser-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Small HTTP service that drives a real headless Chrome (via Playwright) so FlowForge's browserAutomation node can click/type/scrape/screenshot without running a browser inside the queue worker.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.46.0"
  }
}
